# PostgreSQL Store — DS2: Hash Vault + Audit Log + Cognitive Memory
# Tables: hash_vault, audit_log (append-only), episodic_memory, exception_store
# All append-only audit rows are SHA-256 chained for tamper-evidence.
