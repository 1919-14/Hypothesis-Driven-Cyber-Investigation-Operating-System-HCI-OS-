# Dashboard — Flask/React backend
# Endpoints: POST /ingest, GET /hypotheses, GET /audit-log,
#            POST /human-gate/approve, POST /human-gate/revoke, POST /kill-switch
# RBAC views: SOC Analyst, CISO, SysAdmin, Reviewer
